{
  "//": "Main + preload compile unit (Node + Electron). Separate from the renderer so node-side and DOM-side code never share one lib/types set.",
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "types": ["node"]
  },
  "include": ["src/main", "src/preload", "src/shared"]
}
